{
  "name": "codesign-commerce-challenge",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "verify:browser-bundle": "node scripts/verify-browser-bundle.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspaces --if-present && tsc -p packages/codesign-commerce/tsconfig.test.json",
    "check:public-boundary": "node scripts/check-public-boundary.mjs",
    "check:docs": "node scripts/check-doc-links.mjs",
    "check:evals": "node scripts/validate-eval-corpus.mjs && node scripts/score-eval-results.mjs --self-test",
    "score:evals": "node scripts/score-eval-results.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "esbuild": "^0.28.2",
    "happy-dom": "^20.11.6",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
